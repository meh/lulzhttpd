#if !defined(REGEXPP)
#define REGEXPP

#include "String.h"
#include <cstring>
#include <sstream>
#include <vector>

#include <pcre.h>

typedef vector<std::string> Strings;

class Regex
{
    Regex (void);
    Regex (const char* string&, )
};

#endif
